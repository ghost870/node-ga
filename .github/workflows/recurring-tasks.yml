name: Recurring tasks

on:
  schedule:
    - cron: '* * * * *'

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: UnitLogs cleanup
        run: sh .github/workflows-scripts/clean-unitlogs.sh
